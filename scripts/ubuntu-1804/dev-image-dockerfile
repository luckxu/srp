FROM ubuntu:18.04

RUN sed -i 's/archive.ubuntu/mirrors.aliyun/g' /etc/apt/sources.list && \
    sed -i '/security.ubuntu/d' /etc/apt/sources.list && \
    apt update && \
    apt install -y gcc make libssl-dev libevent-dev libmysqlclient-dev && \
    apt clean --dry-run